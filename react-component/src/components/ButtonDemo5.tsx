import Button from "../lib/button";
const ButtonDemo = () => {
    return (
        <div>
            <Button loading>加载中</Button>
            <Button>加载完毕</Button>
        </div>
    )
}

export default ButtonDemo;